{
  "name": "root",
  "private": true,
  "license": "MIT",
  "author": "remorses <beats.by.morse@gmail.com>",
  "contributors": [
    "johnpyp <johnpyp.dev@gmail.com>"
  ],
  "repository": "https://github.com/remorses/",
  "scripts": {
    "test": "NODE_ENV=test vitest",
    "build": "pnpm run --filter '*' build",
    "release": "pnpm run build && changeset",
    "typecheck": "pnpm --recursive --if-present run typecheck",
    "fix": "oxfmt --write && oxlint --fix && pnpm --recursive --if-present run typecheck",
    "secrets:sync": "pnpm --dir website secrets && pnpm --dir otel-collector secrets"
  },
  "dependencies": {
    "oxlint-tsgolint": "^0.17.4",
    "termcast": "link:../termcast/termcast"
  },
  "devDependencies": {
    "@changesets/cli": "^2.30.0",
    "oxfmt": "^0.42.0",
    "oxlint": "^1.57.0",
    "tsx": "^4.21.0",
    "typescript": "^6.0.2",
    "vite": "^8.0.13",
    "vitest": "^4.1.2"
  }
}
